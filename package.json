{
  "name": "logicflow1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/app.js --bundle --outfile=dist/app.js --loader:.css=text",
    "dev": "esbuild src/app.js --bundle --outfile=dist/app.js --loader:.css=text --watch --servedir=.",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@logicflow/core": "^2.2.3",
    "@logicflow/extension": "^2.2.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
